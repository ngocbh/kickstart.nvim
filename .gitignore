tags
test.sh
.luarc.json
nvim

spell/

# Track nvim-pack-lock.json in this fork - see :help vim.pack-lockfile
# (Upstream kickstart ignores it to avoid merge conflicts.)
# nvim-pack-lock.json

.DS_Store
